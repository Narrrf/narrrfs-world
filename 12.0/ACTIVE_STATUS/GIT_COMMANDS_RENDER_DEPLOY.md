# 🚀 Git Commands for Render Deployment

**Branch:** `render-deploy`  
**Date:** November 27, 2025

---

## 📋 **COMMANDS TO RUN**

### **1. Stage All Changes:**
```bash
git add .
```

### **2. Commit with Message:**
```bash
git commit -m "fix: Level 3 & 4 GOD mode initialization - ensure group in scene before spawning

- Added group scene verification to warpToLevel3/4 and restartLevel3/4
- Enhanced spawn functions with comprehensive checks
- Fixed monsters/elements not spawning after GOD mode warp
- Both levels verified working on first attempt"
```

### **3. Push to Render:**
```bash
git push origin render-deploy
```

---

## 🔄 **OR ALL IN ONE (Copy/Paste):**

```bash
git add . && git commit -m "fix: Level 3 & 4 GOD mode initialization - ensure group in scene before spawning" -m "- Added group scene verification to warpToLevel3/4 and restartLevel3/4" -m "- Enhanced spawn functions with comprehensive checks" -m "- Fixed monsters/elements not spawning after GOD mode warp" -m "- Both levels verified working on first attempt" && git push origin render-deploy
```

---

## 📝 **SINGLE LINE VERSION (Easier to Copy):**

```bash
git add . && git commit -m "fix: Level 3 & 4 GOD mode initialization - ensure group in scene before spawning" && git push origin render-deploy
```

---

**Status:** ✅ Ready to execute

